# 人口数据库管理系统 - 共享项目记忆

> 用途：供本仓库中的所有 Agent 和开发者快速对齐项目目标、边界、现状与协作约束。
>
> 更新日期：2026-07-11
>
> 维护方式：实施、联调或验收结论发生变化后，更新本文件中的“当前基线”和对应事项；不要把未验证的设想写成已完成。

## 1. 信息优先级

出现冲突时，按下列顺序判断：

1. `设计约束/人口数据库管理系统_前端开发任务清单与里程碑_V1.0.md`：最新前端里程碑、分工与验收标准；
2. `设计约束/人口数据库管理系统_前端统一开发约束_V1.0.md`：前端页面、权限、交互、组件和视觉统一规则；
3. `项目计划书.docx`、`doc/需求规约.docx`、数据库设计文档：项目背景、总体分工、业务与数据模型依据；
4. 当前代码、接口实现和可重复的构建/测试结果：实际可运行基线。

本文件被 Git 忽略，用作本地协作基准；提交前如需同步其结论，请将必要内容写入受版本控制的文档或提交说明。

## 2. 项目定位与目标

项目为“人口数据库管理系统”，围绕“户 - 人 - 证 - 迁移记录”一体化管理，采用前后端分离架构：

- 前端：Vue 3 + Vite + Element Plus + Pinia + Vue Router + Axios + ECharts；
- 后端：Spring Boot + MyBatis/MyBatis-Plus 风格的 REST API；
- 数据库：MySQL，默认库名 `population_miniterm`；
- 本地服务约定：后端 `http://localhost:8080`，Vite 开发端口偏好 `5180`，`/api` 代理到后端。

最小演示闭环为：

```text
登录
  -> 按三级权限展示菜单与按钮
  -> 人口档案查询/维护
  -> 家庭户与成员维护
  -> 迁入或迁出申请
  -> 查看我的申请
  -> 超级管理员审批
  -> 查看结果、当前户籍与历史归档
```

## 3. 团队分工与前端边界

项目计划书中的总体分工：

- 王家兴：项目计划书、Spring Boot 核心接口、登录、人口/家庭户/迁入迁出/统计等核心功能及联调统筹；
- 赵书亚：需求规约、数据库建表/SQL/测试数据，以及证件、流动人口、重点人口、日志、数据字典等辅助接口；
- 张锦源：前端工程和页面实现、登录页/首页/人口/户籍/迁移/证件/流动人口/重点人口页面及 ECharts 数据展示；
- 全体成员：联调、测试、文档汇总、PPT 与答辩。

最新前端任务清单明确：前端负责页面、交互、权限展示、接口字段对齐和联调；后端/数据库负责真实鉴权、接口权限校验、审批状态流转、迁入迁出及销户事务、当前户籍归档、数据约束、日志写入和文件下载鉴权。

前端不得绕过后端直接改变重大业务状态。销户、注销、跨区迁移、重点业务和敏感导出必须走“申请 - 审批 - 后端办结”流程。

## 4. 不可偏离的前端规则

- 固定三级角色：`NORMAL_USER`（普通用户）、`HOUSEHOLD_ADMIN`（户口管理员）、`SUPER_ADMIN`（超级管理员）。角色名称/代码以最终后端定义为准，前端不可另造一套。
- 普通用户以查询查看为主；户口管理员办理常规业务、提交重大业务申请；超级管理员拥有完整菜单、系统管理和审批权限。
- 未登录必须跳登录；无权限菜单/按钮不可见；手动进入无权限路由必须进入 403；后端仍是最终权限边界。
- 列表默认脱敏身份证号、手机号、详细地址等敏感数据；详情按授权展示。敏感材料和导出文件不得通过公开 URL 暴露。
- 当前户籍与历史归档严格分离；人口页不得直接修改迁出/死亡/注销等重大状态；家庭户页不得直接销户。
- 列表页统一为“标题说明 - 查询区 - 操作栏 - 表格 - 分页 - 抽屉/弹窗”；复杂业务使用独立页或步骤条；关闭未保存表单需确认。
- 统一状态颜色：绿色（正常/有效/通过/办结）、蓝色（草稿/待处理/审批中）、橙色（即将到期/待补充/退回）、红色（驳回/失败/过期/异常）、灰色（停用/注销/撤回/归档）。
- 公共组件应优先沉淀复用：`PageContainer`、`SearchForm`、`CrudTable`、`StatusTag`、选择器、`SensitiveText`、`MaterialUploader`、`ApprovalTimeline`、`DetailDrawer`、`PermissionButton`、`EmptyState` 等；同类功能不得各写一份。
- 所有请求最终统一经 `src/utils/request.ts`（当前实际路径为 `frontend/src/api/request.js`，后续重构须统一，不要新增第三套实例）。

## 5. 里程碑优先级

- M0 / P0-core：工程稳定、清理重复 API、统一分页/请求、环境与构建；
- M1 / P0-core：后台框架、登录恢复、三级菜单/路由/按钮权限、403/404；
- M2 / P0-core：人口、家庭户、成员、选择器、脱敏；
- M3 / P0-core：迁入迁出申请、我的申请、审批工作台、材料、时间线；
- M4 / P1：流动人口、居住凭证、重点人口、证件扩展；
- M5 / P0-demo：工作台、综合查询、ECharts 数据大屏；
- M6 / P1：用户、字典及其他系统页面联调；
- M7 / P0-demo：统一视觉、异常/空态、权限回归、1366x768 与 1920x1080 适配、答辩演练。

时间不足时不可裁剪：三级权限、重大操作申请入口、审批中心、我的申请、敏感数据脱敏、403、登录守卫和核心演示闭环。

## 6. 当前前端基线（2026-07-11）

### 已存在的工程能力

- 分支：`feat/frontend-population`；前端根目录：`frontend/`。
- `npm run build` 已通过（Vite 8.1.3，2026-07-11）。构建仅有依赖注释与主包体积警告：入口 JS 约 1.06 MB（gzip 346.74 kB），暂未阻塞构建。
- 已接入 Vue Router、Pinia、Element Plus 中文语言包、Axios、全局设计 token（政务蓝、紧凑圆角、状态色）。
- 登录信息可存入 `localStorage`，统一请求实例会附带 Bearer Token，401 会清除登录状态并跳转登录页。
- 可复用的已有组件：`SearchPanel`、`AppPagination`、`FormDialog`、`StatusTag`、`PersonSelect`。

### 现有页面/路由

| 状态 | 当前实现 |
| --- | --- |
| 已有 | 登录页 `/login`；工作台 `/home`；人口 `/persons`；家庭户 `/households` 与详情；迁入/迁出 `/migrations/in`、`/migrations/out`；证件 `/certificates`；用户 `/users`。 |
| 已有但仅基础 CRUD | 人口、家庭户、成员、迁入迁出、证件、用户页均以表格 + 查询 + 弹窗表单为主；迁移目前仍是直接新增/删除记录，并非申请审批流程。 |
| 占位 | `/floating-population`、`/key-population`、`/dictionary` 仅显示“正在建设中”。 |
| 未接入路由 | `Home.vue` 是旧的多模块聚合页面，当前路由不使用它；保留时不得与新页面并行维护同一业务逻辑。 |
| 尚未达到约束 | 无分组折叠菜单、面包屑、顶部用户下拉、路由权限、按钮权限、403/404、申请中心、审批中心、当前户籍、历史归档、综合查询、数据大屏与 ECharts 图表。 |

### 已知工程与接口风险

- `frontend/src/api/person.js`、`statistics.js` 自建 Axios 实例；`persons.js` 与 `person.js` 存在重复人口 API。这违反“单一请求实例”目标，应在 M0 统一后再删除冗余文件（删除前必须确认实际引用）。
- `request.js` 仅显式处理 401；尚无 403 跳转、重复错误抑制、下载、请求取消等统一能力。
- Store 当前只保存 `roleName`，没有权限等级、权限码或菜单/按钮权限计算；路由守卫只做登录判断。
- 主布局菜单是固定平铺项，所有已列模块均可见；与三级权限和分组菜单要求不符。
- 当前代码中存在 `auth/register`、`households`、`migrations`、`certificates`、`users` 等前端调用，但当前后端源码只确认有 `/api/auth/login`、`/api/persons`、`/api/residents`、`/api/statistics`、`/api/system/health` 控制器。接口字段、分页格式和真实可用性必须先联调确认，不能仅凭前端 API 文件假设已实现。
- `dashboard.js` 调用的 `/persons/statistics`、`/migrations/in/statistics`、`/certificates/expire-soon` 目前未在已扫描后端 Controller 中确认；数据看板尚未接入 ECharts。
- 多个当前页面直接提供删除、迁入/迁出办理入口；后续重构必须收敛为权限控制与申请审批模型，避免前端直接办结重大操作。

## 7. 推荐的下一步（正式前端构筑顺序）

1. ~~完成 M0~~ / ~~完成 M1~~ / **M2 前端 P0 结构已落地（见第 11 节）**；
2. 后端补齐 `HouseholdController` 后完成家庭户真实联调；
3. 建立“申请详情 + 审批时间线 + 材料上传”公共骨架，推进 M3 迁入迁出/注销/我的申请/审批中心；
4. 后端接口确认后补 M4/M5/M6，最后统一视觉和答辩回归（M7）。

每个阶段结束至少记录：完成项、接口联调证据、权限验证、空/错/加载状态、`npm run build` 结果和未解决风险。

## 8. 当前工作区保护事项

- 当前 Git 工作树已有三个非本次任务产生的已跟踪删除：`模板文件/API（后端功能规范）.md`、`模板文件/Java开发手册（黄山版）1.7.1.docx`、`模板文件/数据库设计书.xlsx`。除非拥有者明确要求，不得恢复、覆盖或提交这些变更。
- 构建产物位于 `frontend/dist/`，受 `frontend/.gitignore` 忽略。
- 前端建议在 `feat/frontend-population` 开发；按约束不直接向 `main` 提交开发代码。提交前必须确认变更范围、构建结果与接口契约。

## 9. M0 已完成基线（2026-07-11）

- 已在 `feat/frontend-population` 完成 M0 工程清理；本地提交为 `cbe49c8 refactor(frontend): 完成 M0 工程清理与基线稳定`，推送状态应以当前 Git 远端为准。
- 删除了无路由入口的旧版 `Home.vue`、重复的 `src/api/person.js` 和无引用 Vite 模板组件/资源；路由首页仍使用 `views/dashboard/Dashboard.vue`。
- 业务 API 统一复用 `src/api/request.js`：`statistics.js` 已去除自建 Axios 与 `localStorage.token`；源码中仅共享请求层保留 Axios 导入。
- 新增 `src/utils/page.js`：页面状态保持 `current`（1 基）和 `size`，请求层转换为 Spring `Pageable` 的 `page`（0 基）与 `size`；列表页统一将 `content`、`totalElements`、`totalPages`、`number` 规范化为 `records`、`total`、`pages`、`current`、`size`。
- 五个现有列表页（人口、户籍、迁入迁出、证件、用户）均使用统一分页工具；工作台 CSS 旧变量已替换为现行设计 token；新增 `.env.example` 并将 `frontend/README.md` 改为项目启动与联调说明。
- 验证：`npm run build` 通过；仅保留既有的 `@vueuse/core` 注释提示和大包体积警告。`/login` 已在浏览器加载，标题正确且控制台无错误；未提供测试账号，因此受保护业务路由未做浏览器冒烟。
- 仍属 M0 范围外的风险：后端当前已确认的控制器与户籍、迁入迁出、证件、用户等前端调用并不完全匹配；不要在未完成接口联调确认前自行修改后端或改变接口路径。

## 10. M1 已完成基线（2026-07-11）

- 已在 `feat/frontend-population` 分支完成 M1 后台框架与三级权限展示改造。
- **权限基础构建**：在 `constants` 目录下定义了 `NORMAL_USER`、`HOUSEHOLD_ADMIN`、`SUPER_ADMIN` 三级角色常量与精细化权限字典树，封装了统一的 `permission.js` 校验函数，并将权限模型缓存于 `pinia/user.js` 中。
- **路由与防线**：改造 `router/index.js`，所有核心业务路由注册完整的 `meta` 权限矩阵，重写全局 `beforeEach` 实现“登录->最低层级->具体权限”三重守卫；针对无权访问与无效链接补齐了 403 和 404 兜底拦截页。
- **界面控件与请求**：新增全局 `v-permission` 权限指令，人口、户籍、迁入迁出、证件与用户管理等各个模块的增删改按钮已悉数接入该指令进行防线管控；在全局 `request.js` 响应层补齐 403 静默拦截机制。
- **框架与导航**：彻底重构 `MainLayout.vue`，实现依权限和分组动态渲染侧边栏树形菜单与面包屑导航，修复了由于 flex-box 内容过宽导致的布局溢出 bug；改进 `Login.vue` 重定向逻辑，实现认证通过后顺畅的页面回跳。
- **验证**：`npm run build` 成功通过；采用前端 localStorage 模拟身份数据，完成普通用户、户籍管理员与超管的视图切换及访问控制测试。
- **M1 遗留风险**：当前的越权拦截完全依赖前端 UI 限定，所有业务变更必须配合后端的角色校验接口。

## 11. M2 已完成基线（2026-07-11）

- 分支：`feat/frontend-population`；起始 commit：`57f5ae7`；**尚未提交**（等待用户确认后再 commit/push）。
- **开工前只读检查**：`git fetch origin` 后对比 `origin/develop`（超前 7 个提交）。develop 已有迁移/注销/归档/审批等模块，但**仍无 `HouseholdController` / 家庭户 CRUD**；有 `ResidenceArchiveController`、`HouseholdArchiveController` 等，属 M3/后续范围，M2 未建假户籍页。
- **人口 P0（可联调 `/api/persons`）**
  - 列表：查询/分页/详情抽屉；身份证与手机号默认脱敏；筛选项对齐后端「正常/已注销」；移除直接删除入口。
  - 表单：无重大状态选择器；编辑强制 `getPersonById`；update 的 `idCard`/`status` 取自最新详情对象。
  - 详情：`PersonDetailDrawer` + `SensitiveText`（`person:sensitive:view` 可临时查看）；关联信息无接口时提示「暂无可用关联信息」。
  - `PersonSelect`：改为 `getPersonPage` 远程防抖搜索，`size≤20`，身份证脱敏，支持 `excludeIds`；已删除 `getPersonList`（全局无引用）。
- **家庭户 P0（前端结构完成 / 接口联调阻塞）**
  - 列表去撤销；编辑不可改户主；详情概览卡 + 成员脱敏 + `isHead` 判断；`HouseholdSelect` 结构完成。
  - **不得标记家庭户业务验收通过**；等待后端 Household API 契约。
- **公共能力**：`mask.js`、`SensitiveText`、`DetailDrawer`、`adapters/person.js`、`adapters/household.js`；权限新增 `person:sensitive:view`（户口管理员默认矩阵）。
- **构建**：`npm run build` 通过（Vite 8.1.3）；仅既有 `@vueuse/core` 注释提示与主包体积警告。
- **安全说明**：前端脱敏不能替代后端字段级脱敏与数据范围控制。
- **P1 跳过**：户主变更专用入口、当前户籍查询页、历史归档查询页（本分支无可用只读契约接入；develop 归档接口属后续阶段）。
- **下一步**：用户确认后提交并仅推送 `feat/frontend-population`；后端补齐家庭户 CRUD 后联调；再进入 M3。
