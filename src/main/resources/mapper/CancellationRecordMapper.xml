<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.population.mapper.CancellationRecordMapper">

    <select id="findByApplicationIdForUpdate" resultType="com.example.population.entity.CancellationRecord">
        SELECT * FROM cancellation_record WHERE application_id = #{applicationId} LIMIT 1 FOR UPDATE
    </select>

    <select id="findByApplicationId" resultType="com.example.population.entity.CancellationRecord">
        SELECT * FROM cancellation_record WHERE application_id = #{applicationId} LIMIT 1
    </select>

    <select id="countOutstandingApplicationsByPerson" resultType="java.lang.Long">
        SELECT COUNT(*) FROM business_application
        WHERE target_person_id = #{personId}
          AND status NOT IN ('COMPLETED','CANCELLED','REJECTED')
    </select>

    <select id="countOutstandingApplicationsByHousehold" resultType="java.lang.Long">
        SELECT COUNT(*) FROM business_application
        WHERE target_household_id = #{householdId}
          AND status NOT IN ('COMPLETED','CANCELLED','REJECTED')
    </select>

    <select id="countCurrentMembers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM household_member
        WHERE household_id = #{householdId} AND member_status = 'CURRENT'
    </select>

</mapper>
