<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjx871.population.resident.ResidentMapper">

    <resultMap id="ResidentResultMap" type="com.wjx871.population.resident.Resident">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="birthDate" column="birth_date"/>
        <result property="idCardNumber" column="id_card_number"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="address" column="address"/>
        <result property="active" column="active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="ResidentColumns">
        id, name, gender, birth_date, id_card_number, phone_number,
        province, city, district, address, active, created_at, updated_at
    </sql>

    <select id="findById" resultMap="ResidentResultMap">
        SELECT
        <include refid="ResidentColumns"/>
        FROM residents
        WHERE id = #{id}
    </select>

    <select id="findByIdCardNumber" resultMap="ResidentResultMap">
        SELECT
        <include refid="ResidentColumns"/>
        FROM residents
        WHERE id_card_number = #{idCardNumber}
    </select>

    <select id="countByIdCardNumber" resultType="long">
        SELECT COUNT(*)
        FROM residents
        WHERE id_card_number = #{idCardNumber}
    </select>

    <select id="countByKeyword" resultType="long">
        SELECT COUNT(*)
        FROM residents
        WHERE #{keyword} IS NULL
           OR LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR id_card_number LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <select id="search" resultMap="ResidentResultMap">
        SELECT
        <include refid="ResidentColumns"/>
        FROM residents
        WHERE #{keyword} IS NULL
           OR LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR id_card_number LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" parameterType="com.wjx871.population.resident.Resident"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO residents (
            name, gender, birth_date, id_card_number, phone_number,
            province, city, district, address, active
        ) VALUES (
            #{name}, #{gender}, #{birthDate}, #{idCardNumber}, #{phoneNumber},
            #{province}, #{city}, #{district}, #{address}, #{active}
        )
    </insert>

    <update id="update" parameterType="com.wjx871.population.resident.Resident">
        UPDATE residents
        SET name = #{name},
            gender = #{gender},
            birth_date = #{birthDate},
            id_card_number = #{idCardNumber},
            phone_number = #{phoneNumber},
            province = #{province},
            city = #{city},
            district = #{district},
            address = #{address},
            active = #{active}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM residents
        WHERE id = #{id}
    </delete>
</mapper>
