<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.population.mapper.AdminRegionMapper">

    <select id="findByRegionCodes" resultType="com.example.population.entity.AdminRegion">
        SELECT * FROM admin_region
        WHERE region_code IN
        <foreach collection="codes" item="code" open="(" close=")" separator=",">
            #{code}
        </foreach>
    </select>

</mapper>
