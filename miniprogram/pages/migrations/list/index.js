const service = require('../../../services/migration')
const adapter = require('../../../adapters/mobile-business')
const { guard } = require('../../../utils/permission')
const { cleanQueryParams } = require('../../../utils/request-params')
const { messageOf } = require('../../../utils/error')
const TYPE_OPTIONS = [{ label: '全部方向', value: '' }, { label: '迁入', value: 'MIGRATION_IN' }, { label: '迁出', value: 'MIGRATION_OUT' }]
Page({ data: { personName: '', applicationNo: '', typeIndex: 0, typeOptions: TYPE_OPTIONS, records: [], page: 0, size: 10, totalDisplay: '数量暂不可用', last: false, loading: false, loadingMore: false, error: '' }, onLoad() { if (this._initialLoadStarted) return; this._initialLoadStarted = true; if (!guard('migration:view')) return this.setData({ error: '当前账号无权查看迁移记录' }); this._authorized = true; return this.load(true) }, inputName(e) { this.setData({ personName: e.detail.value }) }, inputNo(e) { this.setData({ applicationNo: e.detail.value }) }, changeType(e) { this.setData({ typeIndex: Number(e.detail.value) }) }, search() { return this.load(true) }, reset() { this.setData({ personName: '', applicationNo: '', typeIndex: 0 }); return this.load(true) }, async load(reset) { if (!this._authorized || this.data.loading || this.data.loadingMore || (!reset && this.data.last)) return; const page = reset ? 0 : this.data.page + 1; this.setData(reset ? { loading: true, error: '', last: false } : { loadingMore: true }); try { const result = adapter.migrationPage(await service.list(cleanQueryParams({ personName: this.data.personName.trim(), applicationNo: this.data.applicationNo.trim(), migrationType: this.data.typeOptions[this.data.typeIndex].value, page, size: this.data.size }))); this.setData({ records: reset ? result.records : this.data.records.concat(result.records), page: result.number, totalDisplay: result.totalDisplay, last: result.last || result.records.length < this.data.size, error: '' }) } catch (error) { if (reset) this.setData({ records: [], error: messageOf(error) }); else wx.showToast({ title: messageOf(error), icon: 'none' }) } finally { this.setData({ loading: false, loadingMore: false }); wx.stopPullDownRefresh() } }, onPullDownRefresh() { return this.load(true) }, onReachBottom() { return this.load(false) }, open(e) { wx.navigateTo({ url: `/pages/migrations/detail/index?applicationNo=${encodeURIComponent(e.currentTarget.dataset.no)}` }) } })
