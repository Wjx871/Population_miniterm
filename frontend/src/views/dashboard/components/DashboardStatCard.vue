<template><article class="stat-card"><div><p>{{ label }}</p><strong>{{ value === null ? '—' : value }}</strong><small v-if="hint">{{ hint }}</small></div><el-icon v-if="icon"><component :is="icon" /></el-icon></article></template>
<script setup>defineProps({label:String,value:{type:[Number,String],default:null},hint:String,icon:{type:[Object,String],default:null}})</script>
<style scoped>.stat-card{display:flex;justify-content:space-between;min-height:108px;padding:18px;border:1px solid var(--color-border);border-radius:var(--radius-large);background:linear-gradient(135deg,#fff,#f8fbff);box-shadow:var(--shadow-subtle)}p{font-size:13px;color:var(--color-ink-muted)}strong{display:block;margin:8px 0 4px;font-size:28px;color:var(--color-ink)}small{color:var(--color-ink-muted)}.el-icon{font-size:26px;color:var(--color-accent);opacity:.78}</style>
